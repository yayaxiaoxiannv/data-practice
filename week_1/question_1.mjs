/*
* 难度: 简单
* graph模拟数据中, 有各种nodes. x, y 代表node坐标
* 假设把屏幕分成4象限, 设计一个函数,queryNodesOfQuadrants, 对数据进行处理.
* 1. 传入1,2,3,4,筛选出不同象限的节点. queryNodesOfQuadrants的参数自行设计.
* 2. 返回的数据中对value进行处理, 最多保留2位小数
* 3. 将筛选的数据安装x坐标, 从小到大进行排列.
* */
import { nodes } from '../data/graph.mjs';

function queryNodesOfQuadrants() {

    return [];
}
