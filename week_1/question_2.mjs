/*
* 难度: 简单
* graph模拟数据中, 有各种nodes. x, y 代表node坐标
* 1. 将nodes根据category字段进行排序, 排序规则已经给出.
* 2. 不再排序规则中的数据过滤掉
* */
import { nodes } from '../data/graph.mjs';

/* 排序规则,里面是category的值 */
const rules = [7,8,3,2,4,1,0]

function orderNodesWithRule() {

    return [];
}
