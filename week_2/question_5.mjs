/*
* 难度: 中等
* 参考lodash的接口设计,
* 设计tree版本的 map, filter, concatTree, foreach, find 接口,
*
* 要求:
* 1. 尽可能接近lodash的接口设计
* 2. tree的子节点属性名词可以支持动态. 比如 默认叫 children, 但是叫nodes也可以.
* */
import { treeDatas } from '../data/tree.mjs';

function mapTree() {

    return [];
}

function filterTree() {

    return [];
}

function findTree() {

    return [];
}

/*
*  1. 可以考虑下如何设计中断遍历
* */
function forEachTree() {

}

/*
* 将传入的树, 接入到之前的树的某个节点之下
* */
function concatTree() {

    return [];
}
