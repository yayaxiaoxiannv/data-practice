/*
* 难度: 简单
* 设计一个函数对tree进行处理. 你需要做如下几个动作
* 1. 剔除掉value小于500的数据
* 2. value大于5000的数据添加type字段, 值为 vip
* 3. value小于3000的数据添加type字段, 值为 low, 其他的为 normal
* 4. 将整课树展平为对象数组链. 每个对象都有一个 parent id, 指向上级节点, 没有上级就为undefined 就好
*
* 提示:
* 1. 代码清晰可读, 清晰可读, 清晰可读
* */
import { treeDatas } from '../data/tree.mjs';

function tranformNodes() {

    return [];
}
