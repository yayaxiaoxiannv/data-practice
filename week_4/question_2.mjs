/*
* 难度: 中等
* 设计一个函数, 此函数传入一个choice id, 别问id从哪里来? 自己想
* 接上题
*
* 1. 执行选中choice操作以后, 给出一个方案能返回当前所有选中的choice id
* 2. 可以反复执行选中choice操作,选中结果也要正确
*
* 提示:
* 1. 注意反复选中choice, 会对历史的choice选中结果有影响, 比如有一个choice是在另一个choice之下, 上一个choice取消选择, 那他也应该取消选择
* */
import qa from '../data/qa.json'

function toggleChoice( ) {

}
