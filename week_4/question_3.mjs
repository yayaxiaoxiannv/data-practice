/*
* 难度: 中等
* 设计一个函数, 此函数传入一个choice id, 别问id从哪里来? 自己想
* 接上题
*
* 1. 给出一个方案, 让使用者可以指定默认的选中和choice禁用状态
*
* 提示:
* 1. 注意需要和上题保持一致, 默认选中, 也是选中
* */
import qa from '../data/qa.json'

function transformQa( ) {

}
